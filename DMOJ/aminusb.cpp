#include <bits/stdc++.h>
using namespace std;

#define LL long long
#define INF 2e5

int main () {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t,a,b;
    cin >> t;

    while (t--){
      cin >> a >> b;
      cout << a-b<<endl;
    }

    return 0;
}
